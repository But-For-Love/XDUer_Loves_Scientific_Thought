# xdxk-rs 改进清单

## P0

- [x] 校验验证码、登录、课程列表的 HTTP 状态、业务 code 和必要字段。
- [ ] 到选课时间后验证选课/退课接口的业务 code 与响应字段。
- [ ] 到选课时间前确认写接口使用表单 body，并按批次 typeCode 正确生成 chooseVolunteer。
- [ ] 待真实环境确认频控规则后设计重试退避；当前保持原请求节奏。
- [ ] 获取官方“已选课程”接口后，为写超时增加最终状态核验。
- [x] 根据实测 code=401 识别认证失效，并停止只读诊断/扫描。
- [ ] 收集 429/限流及其他业务拒绝的真实响应样本。
- [x] 所有持续任务支持 Ctrl+C 取消，不因单个任务错误或 panic 拖垮整个流程。

## P1

- [ ] 将剩余动态 Value 响应替换为完整 API DTO。
- [x] 使用 thiserror 定义强类型 HTTP/业务错误。
- [ ] 待真实接口确认分页字段和终止条件后实现课程列表分页。
- [x] 配置批次未匹配时明确失败，不再静默回退其他批次。
- [x] 按当前前端协议使用表单 body 登录，并进入 grablessons 页面建立会话。
- [x] 从 grablessons 页面动态解析 TJKC/TYKC/ALLKC 等课程类型。
- [x] 主任务和扫描按真实 clazzType 查询，不再硬编码 FANKC/XGKC。
- [x] 按课程号和课序号匹配 check 目标，避免抢错教学班。
- [ ] 收集真实响应后建立课程操作状态枚举，覆盖成功、已选、已满、候补、冲突、限流和认证失效。
- [ ] 待确认并发请求是否受服务端支持后设计受限调度；当前保持原串行请求顺序。
- [x] 同一教学班仅允许一个进行中的写请求，避免竞态和重复提交。
- [x] 增加只读 inspect 子命令，跨类别诊断 KCH/KXH 和课程数量。
- [x] KCH 匹配兼容空白/大小写，KXH 匹配兼容数字和前导零。
- [x] OCR 无效或验证码错误时有限重试，其他登录错误立即返回。
- [ ] Cookie/Session 封装认证状态；认证过期时明确要求重新登录。
- [ ] 待确认服务端频控后决定是否增加抖动和支持 Retry-After。
- [ ] 待真实环境验证后再拆分 connect 和整体请求超时；当前保留原 15 秒总超时。
- [x] 将 OCR 图片预处理移出 Tokio worker。
- [ ] 依据 ORT Session 线程安全契约消除推理期间的异步锁。

## P2

- [ ] 增加端到端 tracing span 和 request_id；操作事件已记录 course_id、attempt 和 latency_ms。
- [x] 不再丢弃捡漏提交错误，记录最终操作结果。
- [x] 缩减 Tokio features，并使用双 worker CLI 运行时配置。
- [x] 移除生产路径中可能由外部输入触发的 expect。
- [ ] 减少课程列表和配置的不必要 clone。
- [x] 修复 cargo fmt 和 cargo clippy 的全部告警。
- [ ] 增加 HTTP mock 测试，覆盖 401、429、5xx、超时、重复提交、分页和取消。
- [ ] 补充 README、发布打包和使用说明。

## 已完成历史功能

- [x] 对齐选课与退课停止措辞。
- [x] 实现容量检查和捡漏模式。
- [x] 增加全局请求超时。
- [x] 容量检查支持 Ctrl+C。
- [x] 增加 select、drop、check CLI 子命令。
- [x] 轮询间隔可配置。
- [x] 缓存 OCR 会话。
